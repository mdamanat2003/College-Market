{
  "name": "ooplabdh",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && node -e \"const { cpSync, rmSync } = require('fs'); rmSync('public', { recursive: true, force: true }); cpSync('frontend/dist', 'public', { recursive: true });\" && cd backend && npm install && npm run build"
  }
}

{
  "name": "campuscart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx expo export --platform web && cd .. && mkdir public && xcopy frontend\\dist\\* public\\ /E /I /Y && cd backend && npm install && npm run build"
    
  }
}

{
  "name": "campuscart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd campuscart-web && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "bash -c 'cd backend && PORT=3001 npm start & sleep 2 && cd campuscart-web && npm run preview -- --port 5000 --host 0.0.0.0'"
  }
}

{
  "name": "campuscart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx expo export --platform web && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
